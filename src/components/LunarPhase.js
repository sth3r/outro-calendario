import React from 'react';

const LunarPhase = ({ phase }) => {
  // Simple phase display; expand with icons or images if desired
  return <div className="lunar-phase">🌙 {phase}</div>;
};

export default LunarPhase;
